{
  "name": "customer-service-agent",
  "version": "1.0.0",
  "model": "gpt-4",
  "description": "Generates draft emails for customer service requests",
  "prompt_path": "prompts/system.txt",
  "tools": [],
  "inputs": {
    "type": "object",
    "properties": {
      "customer_request": { "type": "string" }
    },
    "required": ["customer_request"]
  },
  "outputs": {
    "type": "object",
    "properties": {
      "draft_email": { "type": "string" }
    },
    "required": ["draft_email"]
  }
}
